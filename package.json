{
  "name": "stopaddict",
  "version": "0.1.0",
  "private": true,
  "description": "StopAddict mobile wrapper",
  "license": "MIT",
  "scripts": {
    "cap:init": "npx cap init stopaddict com.cyberzamalt.stopaddict --web-dir=web --npm-client=npm --yes",
    "cap:add:android": "npx cap add android --yes",
    "cap:sync": "npx cap sync android",
    "cap:copy": "npx cap copy android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  }
}
