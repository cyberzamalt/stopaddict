{
  "name": "stopaddict",
  "version": "0.3.0",
  "private": true,
  "description": "StopAddict mobile wrapper",
  "license": "MIT",
  "scripts": {
    "cap:init": "npx cap init stopaddict com.cyberzamalt.stopaddict --web-dir=web || echo cap-already-inited",
    "cap:add:android": "npx cap add android || echo cap-android-exists",
    "cap:sync": "npx cap sync android",
    "cap:copy": "npx cap copy android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  }
}
